#ifndef TEXCOORDS_H
#define TEXCOORDS_H

// Texture coordinates class
class TexCoords {
public:
    float u, v;

    // Constructors
    TexCoords();
    TexCoords(float u, float v);
};

#endif